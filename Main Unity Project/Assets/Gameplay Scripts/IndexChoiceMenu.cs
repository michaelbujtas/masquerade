using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class IndexChoiceMenu<T> : MonoBehaviour
{
	/*
	public delegate void HandleChoiceDelegate(Choice<T> choice);
	Queue<Choice<T>> decisionQueue = new Queue<Choice<T>>();

	public virtual void GetChoice(T container, HandleChoiceDelegate handle)
	{
		decisionQueue.Enqueue(new Choice<T>(container, handle));
		OnNewChoice();
	}

	protected virtual void OnNewChoice()
	{

	}

	protected class Choice<T>
	{

		public Choice(T container, HandleChoiceDelegate handle)
		{
			Container = container;
			Handle = handle;
		}
		public T Container;
		public HandleChoiceDelegate Handle;
	}*/

}
